# ============================================================
# Grafana - Datasource provisionado automaticamente al arrancar.
# Apunta al contenedor de Prometheus.
# ============================================================
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
